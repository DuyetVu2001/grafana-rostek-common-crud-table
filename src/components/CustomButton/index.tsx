import DownloadButton from './DownloadButton';
import AutoReportButton from './AutoReportButton';
import FilterButton from 'components/CustomButton/FilterButton';

const CustomButton = {
  FilterButton,
  DownloadButton,
  AutoReportButton,
};

export default CustomButton;
